server:
  port: 8761

eureka:
  client:
#    不要使用Eureka服务进行注册（因为这里是服务器端）
    register-with-eureka: false
#    不要在本地缓存注册表信息
  fetch-registry: false
  server:
#    在服务器接收请求之前等待的初始时间
    wait-time-in-ms-when-sync-empty: 5
